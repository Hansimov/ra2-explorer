name: Pages

on:
  push:
    branches:
      - master
    tags:
      - "v*"
    paths:
      - ".github/workflows/pages.yml"
      - "frontend/**"
      - "packaging/pages-data.json"
      - "packaging/pages-cdn.json"
      - "scripts/fetch_pages_snapshot.py"
      - "scripts/publish_pages_cdn.py"
      - "scripts/publish_pages_cdn_set.py"
      - "scripts/privacy_scan.py"
      - "scripts/verify_pages_snapshot.py"
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: github-pages
  cancel-in-progress: true

jobs:
  build:
    runs-on: windows-latest
    defaults:
      run:
        shell: cmd
    steps:
      - name: Check out repository
        uses: actions/checkout@v6
        with:
          fetch-depth: 0
      - name: Configure GitHub Pages
        uses: actions/configure-pages@v5
      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version: "3.13"
      - name: Set up Node
        uses: actions/setup-node@v6
        with:
          node-version: "22"
          cache: npm
          cache-dependency-path: frontend/package-lock.json
      - name: Download pinned slim data
        run: python scripts\fetch_pages_snapshot.py --output .pages-data\RA2-Explorer-Pages-Data.zip
      - name: Verify archive before extraction
        run: python scripts\verify_pages_snapshot.py .pages-data\RA2-Explorer-Pages-Data.zip --lock packaging\pages-data.json
      - name: Build static application
        working-directory: frontend
        run: |
          call npm ci
          call npm run build:pages
      - name: Assemble and verify site
        run: |
          if not exist frontend\dist-pages\index.html exit /b 1
          if not exist frontend\dist-pages\assets exit /b 1
          mkdir frontend\dist-pages\data
          tar -xf .pages-data\RA2-Explorer-Pages-Data.zip -C frontend\dist-pages\data
          copy /y NUL frontend\dist-pages\.nojekyll >NUL
          if not exist frontend\dist-pages\data\manifest.json exit /b 1
          python scripts\verify_pages_snapshot.py frontend\dist-pages\data
      - name: Upload GitHub Pages artifact
        uses: actions/upload-pages-artifact@v4
        with:
          path: frontend/dist-pages

  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: windows-latest
    needs: build
    steps:
      - name: Deploy GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
